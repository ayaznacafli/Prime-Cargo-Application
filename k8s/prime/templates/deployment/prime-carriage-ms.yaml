apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}-carriage-ms
spec:
  replicas: {{ .Values.services.carriageMs.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "fullname" . }}-carriage-ms
  template:
    metadata:
      labels:
        app: {{ template "fullname" . }}-carriage-ms
    spec:
      nodeSelector:
      #        environment: {{ .Release.Name }}
      containers:
        - name: {{ template "fullname" . }}-carriage-ms
          image: {{ .Values.services.carriageMs.image }}:{{ .Values.services.carriageMs.tag }}
          resources:
            limits:
              memory: {{ .Values.services.carriageMs.limits.memory }}
            requests:
              memory: "400Mi"
          ports:
            - containerPort: {{ .Values.services.carriageMs.port }}
          env:
            - name: DB_CONNECTION_IP
              value: {{ template "fullname" . }}-mysql